(** {1 Standard Library} *)

module Fun = Fun

(** {2 Constructions} *)

module Functor = Functor
module Applicative = Applicative
module Monad = Monad

(** {2 Incarnations} *)

module Identity = Identity
